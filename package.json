{
  "name": "gastos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run": "cypress run --reporter mochawesome --reporter-options reportDir=mochawesome-report,overwrite=false,html=false,json=true",
    "merge-reports": "mochawesome-merge mochawesome-report/*.json > mochawesome.json",
    "generate-report": "marge mochawesome.json -f index.html -o mochawesome-report",
    "report:full": "npm run merge-reports && npm run generate-report",

      
    "vercel-build": "pip install -r requirements.txt && mkdocs build --clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.34.1",
    "cypress": "^15.3.0",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.3.0"
  }

}
