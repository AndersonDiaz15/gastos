name: CI - Cypress + Mochawesome + Netlify

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    env:
      CYPRESS_SPEC: "cypress/e2e/**/*.cy.js"

    steps:
      # 1️⃣ Descargar el repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Instalar Node
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Instalar dependencias
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Ejecutar Cypress y generar reporte Mochawesome
      - name: Run Cypress Tests
        run: |
          # Ejecuta Cypress sin detener workflow aunque haya fallos
          npx cypress run --browser chrome --spec "${{ env.CYPRESS_SPEC }}" \
            --reporter mochawesome \
            --reporter-options reportDir=mochawesome-report,overwrite=false,html=false,json=true || true

          # Genera reporte consolidado
          npx mochawesome-merge mochawesome-report/*.json > mochawesome.json
          npx marge mochawesome.json --reportDir mochawesome-report --inline

          # Renombrar HTML para Netlify
          mv mochawesome-report/mochawesome.html mochawesome-report/index.html

      # 5️⃣ Instalar Netlify CLI
      - name: Install Netlify CLI
        run: npm install netlify-cli -g

      # 6️⃣ Desplegar en Netlify
      - name: Deploy to Netlify
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        run: |
          netlify deploy --dir=mochawesome-report --prod

      # 7️⃣ Enviar el link por correo
      - name: Send Netlify Report Link
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "📊 Reporte Cypress - ${{ github.repository }} #${{ github.run_number }}"
          to: ${{ secrets.EMAIL_TO }}
          from: "CI GitHub Actions <${{ secrets.EMAIL_USER }}>"
          body: |
            Hola,

             REPORTE DE EJECUCIÓN DE PRUEBAS CYPRESS

          Repositorio: ${{ github.repository }}
          Rama: ${{ github.ref_name }}
          Commit: ${{ github.event.head_commit.message }}
          Autor: ${{ github.event.head_commit.author.name }}
          Estado: ${{ job.status | upper }}

          El reporte completo está disponible en:
          https://gastos-reportes.netlify.app

          ¡Saludos!
          Equipo de QA

    # Guardar videos si falla Cypress
    # - name: Upload Cypress videos
    #   if: failure()
    #   uses: actions/upload-artifact@v4
    #   with:
    #    name: cypress-videos
    #     path: cypress/videos

    # Guardar screenshots si falla Cypress
    #  - name: Upload Cypress screenshots
    #    if: failure()
    #    uses: actions/upload-artifact@v4
    #    with:
    #      name: cypress-screenshots
    #      path: cypress/screenshots

    # Subir archivos vía FTP solo si Cypress pasó
    # - name: Deploy to InfinityFree via FTP
    #   if: success() # corre solo si los tests pasaron
    #   uses: SamKirkland/FTP-Deploy-Action@4.3.0
    #   with:
    #     server: ${{ secrets.FTP_HOST }}
    #     username: ${{ secrets.FTP_USER }}
    #     password: ${{ secrets.FTP_PASSWORD }}
    #     local-dir: ./ # Carpeta local a subir
    #     server-dir: /htdocs/ # Carpeta remota en InfinityFree
