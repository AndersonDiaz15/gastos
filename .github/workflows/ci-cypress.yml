name: CI - Cypress + Mochawesome

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    env:
      CYPRESS_SPEC: "cypress/e2e/**/*.cy.js"

    steps:
      # 1️⃣ Descargar el repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Instalar Node
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Instalar dependencias
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Ejecutar Cypress (aunque fallen tests, continua el flujo)
      - name: Run Cypress tests
        run: npm run cy:run || true

      # 5️⃣ Merge y generar reporte Mochawesome
      - name: Generate Mochawesome Report
        run: npm run report:full

      # 6️⃣ Publicar reporte en GitHub Pages (repo externo o mismo repo)
      - name: Deploy Mochawesome Report
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.REPORTES_TOKEN }}
          external_repository: AndersonDiaz15/gastos-reports # Cambia si es otro repo
          publish_branch: gh-pages
          publish_dir: ./mochawesome-report
          user_name: "GitHub Actions"
          user_email: "actions@github.com"
          commit_message: "🚀 Publicación automática del reporte Mochawesome"

      # Guardar videos si falla Cypress
      # - name: Upload Cypress videos
      #   if: failure()
      #   uses: actions/upload-artifact@v4
      #   with:
      #    name: cypress-videos
      #     path: cypress/videos

      # Guardar screenshots si falla Cypress
      #  - name: Upload Cypress screenshots
      #    if: failure()
      #    uses: actions/upload-artifact@v4
      #    with:
      #      name: cypress-screenshots
      #      path: cypress/screenshots

      # Subir archivos vía FTP solo si Cypress pasó
      # - name: Deploy to InfinityFree via FTP
      #   if: success() # corre solo si los tests pasaron
      #   uses: SamKirkland/FTP-Deploy-Action@4.3.0
      #   with:
      #     server: ${{ secrets.FTP_HOST }}
      #     username: ${{ secrets.FTP_USER }}
      #     password: ${{ secrets.FTP_PASSWORD }}
      #     local-dir: ./ # Carpeta local a subir
      #     server-dir: /htdocs/ # Carpeta remota en InfinityFree
