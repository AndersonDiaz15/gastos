name: CI - Cypress + Allure + Pages

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    env:
      CYPRESS_SPEC: "cypress/e2e/**/*.cy.js"

    steps:
      # 1️⃣ Descargar el código
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Configurar Node.js
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Instalar dependencias
      - name: Install dependencies
        run: npm ci

      - name: Instalar plugin Allure
        run: npm install @shelex/cypress-allure-plugin --save-dev

      # 4️⃣ Ejecutar Cypress y generar reporte Allure
      - name: Run Cypress Tests with Allure
        run: |
          npx cypress run --browser chrome --spec "${{ env.CYPRESS_SPEC }}"
          npx allure generate allure-results --clean -o allure-report

      # 5️⃣ Desplegar el reporte a GitHub Pages
      - name: Deploy Allure Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: allure-report

      # 6️⃣ Enviar correo con el enlace al reporte
      - name: Send Allure Report Link by Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "📊 Reporte Cypress - ${{ github.repository }} #${{ github.run_number }}"
          to: "anjodivi15@gmail.com"
          from: "CI GitHub Actions <${{ secrets.EMAIL_USER }}>"
          body: |
            🚀 **Resultados de la ejecución de pruebas Cypress**

            **Repositorio:** ${{ github.repository }}
            **Rama:** ${{ github.ref_name }}
            **Commit:** ${{ github.event.head_commit.message }}
            **Autor:** ${{ github.event.head_commit.author.name }}
            **Resultado:** ${{ job.status }}

            📈 Visualiza el reporte Allure en:
            🔗 https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/

      # Guardar videos si falla Cypress
      # - name: Upload Cypress videos
      #   if: failure()
      #   uses: actions/upload-artifact@v4
      #   with:
      #    name: cypress-videos
      #     path: cypress/videos

      # Guardar screenshots si falla Cypress
      #  - name: Upload Cypress screenshots
      #    if: failure()
      #    uses: actions/upload-artifact@v4
      #    with:
      #      name: cypress-screenshots
      #      path: cypress/screenshots

      # Subir archivos vía FTP solo si Cypress pasó
      # - name: Deploy to InfinityFree via FTP
      #   if: success() # corre solo si los tests pasaron
      #   uses: SamKirkland/FTP-Deploy-Action@4.3.0
      #   with:
      #     server: ${{ secrets.FTP_HOST }}
      #     username: ${{ secrets.FTP_USER }}
      #     password: ${{ secrets.FTP_PASSWORD }}
      #     local-dir: ./ # Carpeta local a subir
      #     server-dir: /htdocs/ # Carpeta remota en InfinityFree
